def add_noise(images: list[str], noise_level: float) -> list[str]:
    """ノイズを加える

    Args:
        images (list[str]): 画像のパスのリスト
        noise_level(float): ノイズの値

    Returns:
        list[str]: ノイズが加えられた画像のパスのリスト
    """
    return []
